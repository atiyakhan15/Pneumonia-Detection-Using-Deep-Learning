Input: 1 × 28 × 28
   │
Conv2D (1 → 16, 3×3, stride=1) + ReLU
→ 16 × 26 × 26
   │
Conv2D (16 → 16, 3×3, stride=2) + ReLU
→ 16 × 12 × 12
   │
Conv2D (16 → 32, 3×3, stride=1) + ReLU
→ 32 × 10 × 10
   │
Conv2D (32 → 32, 3×3, stride=2) + ReLU
→ 32 × 4 × 4
   │
Flatten
→ 512
   │
Fully Connected (512 → 1)
   │
Output: Binary logit (Normal / Pneumonia)



| Layer                | Kernel / Units | Output Shape | Parameters |
| -------------------- | -------------- | ------------ | ---------- |
| Conv1                | 3×3, 1→16      | 16×26×26     | 160        |
| Conv2                | 3×3, 16→16     | 16×12×12     | 2,320      |
| Conv3                | 3×3, 16→32     | 32×10×10     | 4,640      |
| Conv4                | 3×3, 32→32     | 32×4×4       | 9,248      |
| FC                   | 512→1          | 1            | 513        |
| **Total Parameters** |                |              | **≈ 16K**  |
